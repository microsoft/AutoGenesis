{
    "PYWINAUTO_CONFIG": {
        "apps_config": {
            "# app name": {
                "exe": "# app exe path",
                "window_title_re": "# window title regex",
                "launch_args": ["# app launch arguments, e.g. --remote-debugging-port for edge"]
            }
        }
    }
}
